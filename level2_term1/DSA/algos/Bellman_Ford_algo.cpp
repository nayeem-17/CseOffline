#include <bits/stdc++.h>
using namespace std;

#define MAX_N 5
#define EMPTY -1
int edges[MAX_N][MAX_N];
int dp[MAX_N];
int shortest_path(int n, int source, int target)
{
}
int main(int argc, char const *argv[])
{
    memset(edges, EMPTY, sizeof(edges[0][0]) * MAX_N * MAX_N);
    memset(dp, EMPTY, sizeof(dp));
    return 0;
}
